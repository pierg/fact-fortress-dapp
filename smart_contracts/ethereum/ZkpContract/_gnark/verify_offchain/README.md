# Gnark: Verify Offchain [WIP]

Verify the proof offchain

## Run

(ensure to have run `generate` and `prove` beforehand).

```
go run main.go
```

## Expected output [WIP]

* stdout: `verification -> accept` or `verification -> reject (<error>)`
